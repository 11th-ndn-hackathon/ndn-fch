---
name: build
on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/setup-go@v2
        with:
          go-version: "^1.16"
      - uses: actions/checkout@v2
      - run: go build ./...
      - run: go test ./...
